subprojects {
  apply plugin: 'java'

  group = 'com.palominolabs'
  version = '1.0.0-SNAPSHOT'

  sourceCompatibility = JavaVersion.VERSION_1_6
  targetCompatibility = JavaVersion.VERSION_1_6

  dependencies {
  }

  repositories {
    mavenCentral()
    mavenLocal()
  }
}
