subprojects {
  apply plugin: 'java'

  group = 'com.palominolabs.benchmark'
  version = '1.0.0-SNAPSHOT'

  sourceCompatibility = JavaVersion.VERSION_1_8

  repositories {
    mavenCentral()
    mavenLocal()
  }
}

task wrapper(type: Wrapper) {
  gradleVersion = 1.10
}
